import NavBar from "../lib/components/navBar";

export default function Login() {
    return (
        <div>
            <NavBar />
            <h1>Login Page</h1>
            {/* ...existing styling and code... */}
        </div>
    );
}
